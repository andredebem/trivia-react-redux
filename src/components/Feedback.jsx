import React, { Component } from 'react';
// import PropTypes from 'prop-types'

export default class Feedback extends Component {
  render() {
    return (
      <span data-testid="feedback-text">Mensagem de Feedback</span>
    );
  }
}
